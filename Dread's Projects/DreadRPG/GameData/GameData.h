#ifndef GAMEDATA_H
#define GAMEDATA_H

#include "../includes.h"

class GameData
{
    //Player* player;
public:
    GameData();
    GameData(const GameData& orig);
    
    virtual ~GameData();

private:

};

#endif